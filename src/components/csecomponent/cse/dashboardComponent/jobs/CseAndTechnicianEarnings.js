import GlobalModal from "../../../../globalcomponents/GlobalModal";

const CseAndTechnicianEarning = ({ isOpen, closeModal }) => {
  return <GlobalModal isOpen={isOpen} closeModal={closeModal}></GlobalModal>;
};

export default CseAndTechnicianEarning;
